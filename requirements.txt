# Для GitHub Actions и локальных запусков. Тот же список, что в pyproject.toml
# (Vercel читает его), плюс python-dotenv — он нужен только локально, чтобы
# подхватить .env.local.
#
# lxml убран намеренно: parser.py при его отсутствии берёт встроенный
# html.parser. На живом сайте результат совпадает побайтово, разница по
# времени 0.04 с против 0.06 с на разбор таблицы, а на Vercel компиляция
# колеса — лишний риск для сборки.
requests>=2.31
beautifulsoup4>=4.12
tzdata>=2024.1
python-dotenv>=1.0
