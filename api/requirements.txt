# Зависимости именно вебхука. lxml тут нет намеренно: parser.py при его
# отсутствии берёт встроенный html.parser, результат тот же (проверено),
# а сборка на Vercel не рискует упасть на компиляции колеса.
requests>=2.31
beautifulsoup4>=4.12
tzdata>=2024.1
